version: "3.7"

networks:
    laravel-tce:

services:

    php:
        container_name: tce-php
        build: docker/php
        working_dir: /var/www/tce
        volumes:
            - ./:/var/www/tce
        extra_hosts:
            - "host.docker.internal:host-gateway"
